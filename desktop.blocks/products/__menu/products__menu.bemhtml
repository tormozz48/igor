block('products').elem('menu')(

    def().match(!this.ctx.content).match(this.ctx.content.items)(function() {
        return false;
    }),

    content()(function() {
        return {
            block: 'menu',
            content: this.ctx.content
        }
    })
)