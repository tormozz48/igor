block('sticker')(

    def().match(!this.ctx.content)(function() {
        return false;
    }),

    content()(function() {
        var c = this.ctx.content;
        return [
            { elem: 'title', content: c.title },
            { elem: 'body', icon: c.icon, content: applyNext() }
        ]
    })
)