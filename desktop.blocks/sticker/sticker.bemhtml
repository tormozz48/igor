block('sticker')(
    content()(function() {
        return [
            { elem: 'title', content: this.ctx.title },
            { elem: 'body', icon: this.ctx.icon, content: applyNext() }
        ]
    })
)