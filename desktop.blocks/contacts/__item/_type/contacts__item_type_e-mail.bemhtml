block('contacts').elem('item').elemMod('type', 'e-mail')(

    elem('value').content()(function() {
        return {
            block: 'link',
            url: 'mailto:' + this.ctx.content,
            content: this.ctx.content
        };
    })

)