block('product')(

	content().match(this.ctx.tables)(function() {
		var buf = this.ctx.tables.map(function(item) {
		    return {
		        elem: 'table',
		        content: item
		    }
		});

		buf.unshift({ elem: 'title', content: this.ctx.name });

		return buf;
	})
)